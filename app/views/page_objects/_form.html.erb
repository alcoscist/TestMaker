<%= form_for(@page_object) do |f| %>
  <% if @page_object.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page_object.errors.count, "error") %> prohibited this page_object from being saved:</h2>

      <ul>
      <% @page_object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label :tagName %><br>
    <%= f.text_area :tagName %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
